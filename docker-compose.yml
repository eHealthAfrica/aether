core:
  build: .
  volumes:
          - .:/opt/
  net: "host"
  command: bash /opt/gather2-core/entrypoint.sh  manage runserver 0.0.0.0:8000
  privileged: true

db:
  image: "postgres:9.4"
  net: "host"

