#!/bin/bash
set -e

# Production specific functions

check_variable() {
  if [ -n "$1" ];
  then
    echo "$2 in Ordnung!"
  else
    echo "Missing $2"
    exit -1
  fi
}

check_variable $ADMIN_PASSWORD      "ADMIN password"
check_variable $AETHER_KERNEL_TOKEN "ADMIN token"
