kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: {{ .Chart.Name }} 
spec:
  accessModes:
    - ReadWriteMany
  local:
    path: /tmp/test
  resources:
    requests:
      # This is not used by the provisioner, but is required by the PVC.
      storage: 1Mi
