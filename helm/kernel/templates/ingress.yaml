{{- if .Values.provider.aws -}}
{{- $serviceName := include "kernel.name" . -}}
{{- $servicePort := .Values.service.externalPort -}}
apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: {{ template "kernel.name" . }}
  labels:
    app: {{ template "kernel.name" . }}
    chart: {{ .Chart.Name }}-{{ .Chart.Version | replace "+" "_" }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
  annotations:
    {{- range $key, $value := .Values.ingress.annotations }}
      {{ $key }}: {{ $value | quote }}
    {{- end }}
spec:
  rules:
    {{- range $host := .Values.ingress.hosts }}
    - host: {{ $host }}
      http:
        paths:
          - path: /
            backend:
              serviceName: {{ $serviceName }}
              servicePort: {{ $servicePort }}
    {{- end -}}
{{- else -}}
apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: {{ template "kernel.name" . }}
  labels:
    app: {{ template "kernel.name" . }}
    chart: {{ .Chart.Name }}-{{ .Chart.Version | replace "+" "_" }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
  annotations:
    {{- range $key, $value := .Values.ingress.annotations }}
      {{ $key }}: {{ $value | quote }}
    {{- end }}
spec:
  tls:
  - hosts:
      - kernel.aether.local
  rules:
    - host: kernel.aether.local
      http:
        paths:
          - path: /
            backend:
              serviceName: kernel
              servicePort: 80
{{- end -}}
